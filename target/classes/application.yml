server:
  port: 9191

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/registration_db
    username: root
    password: Harish@12
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
        connection.url: jdbc:mysql://localhost:3306/registration_db
    hibernate:
      ddl-auto: update
    show-sql: true
